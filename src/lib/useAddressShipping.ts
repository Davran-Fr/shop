// import { ShippingType } from "@/validation/shipping";

// export const useSaveAddress = (token: ShippingType) => {
//   if (typeof window !== "undefined") {
//     localStorage.setItem("addresses", token);
//   }
// };
